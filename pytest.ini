[pytest]
faulthandler_timeout = 300
markers =
    agentkit: Spec Kit workflow tests
    integration_ci: Hermetic integration tests (Docker Compose-backed, no external credentials)
    provider_verification: Tests requiring real third-party provider credentials
    jules: Jules-related tests
    requires_credentials: Tests requiring external service credentials
