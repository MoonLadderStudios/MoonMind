name: Provider / Omnigent Native Chat Acceptance

on:
  workflow_dispatch:
    inputs:
      evidence_artifact:
        description: "Artifact containing native-chat-acceptance-input.json (deterministic + protected-live matrix)"
        required: true
        type: string
      evidence_run_id:
        description: "Workflow run that produced the matrix artifact"
        required: true
        type: string

permissions:
  contents: read
  actions: read

concurrency:
  group: omnigent-native-chat-acceptance
  cancel-in-progress: false

jobs:
  publish:
    name: Validate and publish the native Workflow Chat rollout gate
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
      issues: write
    steps:
      - name: Checkout exact candidate commit
        uses: actions/checkout@v7
      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Download complete matrix evidence
        uses: actions/download-artifact@v8
        with:
          name: ${{ inputs.evidence_artifact }}
          run-id: ${{ inputs.evidence_run_id }}
          github-token: ${{ github.token }}
          path: artifacts/omnigent-native-chat-acceptance/input
      - name: Build native chat acceptance report
        run: |
          python tools/build_native_chat_acceptance.py \
            artifacts/omnigent-native-chat-acceptance/input/native-chat-acceptance-input.json \
            artifacts/omnigent-native-chat-acceptance/report.json \
            --evidence-root artifacts/omnigent-native-chat-acceptance/input/evidence \
            --expected-commit "${{ github.sha }}"
      - name: Upload passing report
        uses: actions/upload-artifact@v7
        with:
          name: omnigent-native-chat-acceptance-${{ github.run_id }}-${{ github.run_attempt }}
          path: artifacts/omnigent-native-chat-acceptance/report.json
          if-no-files-found: error
          retention-days: 90
      - name: Link passing report from issue 3642
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          report_artifact="omnigent-native-chat-acceptance-${{ github.run_id }}-${{ github.run_attempt }}"
          run_url="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          comment_file="${RUNNER_TEMP}/omnigent-native-chat-acceptance.md"
          {
            echo "Native Omnigent Workflow Chat acceptance gate: PASS"
            echo
            echo "- Run: ${run_url} (attempt ${{ github.run_attempt }})"
            echo "- Commit: \`${{ github.sha }}\`"
            echo "- Durable report: \`${report_artifact}\`"
            echo "- Source matrix: \`${{ inputs.evidence_artifact }}\` from run \`${{ inputs.evidence_run_id }}\`"
          } > "$comment_file"
          gh issue comment 3642 --repo "${{ github.repository }}" --body-file "$comment_file"
