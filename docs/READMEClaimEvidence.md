# README Claim/Evidence Checklist (MoonMind#3968)

This is a small editorial checklist for the README change in
MoonMind#3968. It is **not** a runtime registry and creates no new
authority: each row points at the canonical owner that enforces the
behavior. Fix inaccurate README prose from this table immediately
rather than waiting for every planned feature to land.

| # | README claim (qualified form) | Enforced boundary | Runtime / credential / topology | Default or opt-in | Production caller | Verification test / evidence | Limitation |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | Runs are constrained by policy-enforced boundaries, not by agent self-restraint | Egress profile attestation + capability routing; no Docker socket for agent runtimes | Managed sessions, Container Jobs, Omnigent hosts on deployment-owned internal networks | Enforced by default where the profile is attested | Trusted agent-runtime worker; API-owned Docker Backend Service | `tests/integration/security/test_restricted_egress_live.py` (opt-in conformance, `MOONMIND_RUN_EGRESS_CONFORMANCE=1`); attested `enforcedNetworkRefs` | Plain `bridge`, `control-plane-network`, and local allowlists are non-enforcing; not every surface is confined — see [Restricted egress](./Security/RestrictedEgress.md) |
| 2 | Durable contracts carry secret references, not raw values | `SecretRef` in workflow payloads, profiles, scheduler state, run metadata | All runtimes; resolution only at controlled launch boundaries | Default | Resolver at provider-profile-backed launch and MoonMind-owned outbound calls | Unit suites around `moonmind/security/outbound_scan.py`; audit fields without plaintext — see [Secrets System](./Security/SecretsSystem.md) | No guarantee that raw secrets never exist in process memory (explicit non-goal); materialized runtime files are ephemeral, not artifacts |
| 3 | High-security mode scans outbound text before send/post/push | Canonical bundle scanner on MoonMind-owned send-message and publish paths | Opt-in via `MOONMIND_HIGH_SECURITY_MODE` (default `false`), overridable per call | **Opt-in** | MoonMind-owned outbound callers; native Omnigent facade | Outbound-scan unit tests; blocked scans emit redacted category + caller-provided location only | Binary attachments, terminal input, and browser automation are outside the native text-scan contract; disabled mode allows payloads through unchanged |
| 4 | Temporal orchestration resumes when infrastructure returns | Durable step ledger, persisted attempts/outputs, replay-safe workers | Temporal + PostgreSQL persistence; host running the stack must be up | Default where Temporal runs | Workflow workers; step-boundary checkpoint restore | Replay/compatibility suites, e.g. `tests/unit/workflows/temporal/test_run_replayer.py` | Suspending the hosting machine suspends execution; resume needs compatible workspace capture/restore evidence — see [Execution Model](./Temporal/ManagedAndExternalAgentExecutionModel.md) §16–17 |
| 5 | Retries reuse idempotency keys and inspect durable state before new side effects | `idempotencyKey` / deterministic execution tuple per start-like side effect | All execution lanes (external, direct managed, Omnigent hybrid) | Default | Workflow/activity retry path | Idempotency and lifecycle-ordering CI; lane reconciliation rules in [Execution Model](./Temporal/ManagedAndExternalAgentExecutionModel.md) §16 | Ambiguous provider responses require reconciliation; no lane creates replacement authority while the original may be active — not universal exactly-once |
| 6 | Verified objectives survive reporting failures | `separate_objective_from_execution`: objective state kept distinct from execution result | GitHub-issue finalization workflow | Default | `moonmind/workflows/temporal/github_issue_finalization.py` | Finalization unit tests | Applies to verified implementation/merge vs auxiliary reporting failures only |
| 7 | Support is per exact combination, evidence-gated | Rollout states (`new_work_default`, `explicit_only`, `direct_compatibility_only`, `retired_for_new_work`, …) over thirteen exact dimensions plus path class | Per harness × provider × host class × pack × materializer × model × host mode × … | Built-in rows gated by qualification env flags (e.g. `MOONMIND_OMNIGENT_GENERIC_CODEX_QUALIFIED`) | `resolve_runtime_target_selection`; frozen into the immutable execution plan | Rollout unit tests; migration status `GET /api/omnigent/runtime-provider-migration` | A present binary/image is "implemented", never "supported" — see [Rollout Policy](./Omnigent/RuntimeProviderRollout.md) and [Codex Support and Cutover](./Omnigent/CodexSupportAndCutover.md) |
| 8 | Observability views show existing evidence; the consolidated governance report is planned | Dashboard, SSE live logs, content-addressed artifacts, correlated logs, runtime provenance | Current views ship with the Compose deployment | Default | API service, dashboard, artifact store (MinIO) | Live-log, artifact, and provenance suites | Missing/partial evidence must not be advertised as a complete audit; per-run governance report is planned — see [RunGovernanceReports](./Governance/RunGovernanceReports.md) (MoonMind#3969) |

## How to use this table

1. Before rewording a README promise, find its row above.
2. State the enforced boundary, the default/opt-in condition, and the limitation in the prose.
3. Link the canonical owner doc, not this checklist, as the evidence.
4. If the behavior changes, update the owner doc and its tests first, then this row.
