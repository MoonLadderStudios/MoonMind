# Manifest Producer/Disposition Ledger — #4188 (MR1)

Parent: MoonLadderStudios/MoonMind#4187. Plan: `docs/tmp/ManifestSystemRemovalPlan.md`
(MR1). Baseline rechecked at implementation revision (post-`b20cf48`).
Method: symbol/alias search over `api_service/`, `moonmind/`, `scripts/`,
`tools/`, `frontend/src` for `ManifestIngest`, `manifest_run`,
`manifest_ref`/`manifestArtifactRef`, `entry=manifest`/`entry:manifest`,
and `manifest` CLI/route/nav surfaces; unrelated retained systems
(recovery/Skill/Vite/build manifests) excluded per the removal plan §3.

Status rule: no application entrypoint can create or reactivate native
Manifest work (`MoonMind.ManifestIngest`, legacy `manifest_run` kind).
Rejection precedes registry mutation, artifact creation/processing,
reader/network access, compile, Temporal start, host launch, and child work,
at the earliest existing shared unsupported-type/capability boundary.
No second admission engine; existing error vocabulary reused.

| # | Source symbol / surface | Role | Persisted representation | Authority / effect boundary (first reject) | Disposition + implementing child | Test obligation |
|---|---|---|---|---|---|---|
| P1 | `api_service/api/routers/manifests.py` (`/api/manifests`, run submission, worker state callback) — REMOVED | producer (dedicated registry + submit + callback) | `manifest` table / `ManifestRecord` (dropped via `376_drop_manifest_registry_4192.py` after protected export) | N/A — surface absent; final paths return ordinary unsupported-route behavior | Remove router + schemas/registration; retain only MR2 old-consumer drain controls under `manifest_ingest_drain` gate. Child: MR1 (#4188) | `test_retired_manifest_status_route_is_gone`, `test_retired_manifest_nodes_route_is_gone`, `test_retired_manifest_routes_are_gone`, dashboard `/manifests` null-route |
| P2 | `api_service/services/manifests_service.py` (YAML artifact creation + shared execution invoke) — REMOVED | producer (dedicated submit) | Manifest YAML artifacts | N/A — module absent; shared boundary below is the guard for any alias caller | Remove service. Child: MR1 (#4188) | Absence guards + shared rejection tests (P3) prove no artifact/compile side effect |
| P3 | Shared `POST /api/executions` (`CreateExecutionRequest` in `moonmind/schemas/temporal_models.py:2609`, `TemporalExecutionService.create_execution` in `moonmind/workflows/temporal/service.py:2040`) | producer (inline + registry + copied-input + draft + preset + integration submission) | `temporal_executions` canonical rows (immutable `workflow_type` string retained for history) | Schema `_validate_required_fields` then service `create_execution` reject `MoonMind.ManifestIngest` actionably before artifact/reader/compile/Temporal/host/child effects | Retire intent at existing boundary; never strip fields and run different work. Child: MR1 (#4188); drain ownership MR2 (#4189) | `test_create_execution_rejects_retired_manifest_ingest` (schema + service), production-ingress no-side-effect tests |
| P4 | Updates / reset / resume (`send_update` retired-update rejection `service.py:2730`, `rerun_execution` via `create_execution`, `_build_action_capabilities` disables `can_update_inputs`/`can_edit_for_rerun`/`can_rerun` for non-`UserWorkflow` with `unsupported_workflow_type`) | control (update/rerun/reset/resume) | Existing execution rows (read-only history) | `send_update` rejects retired update names; rerun re-enters `create_execution` → 422; UI affordances disabled | Historical executions read-only via MR2; no rerun/retry-node/update/resume/reset recreation. Child: MR1 + MR2 | `test_retired_manifest_update_is_rejected_actionably`, rerun-rejection coverage, capability `unsupported_workflow_type` assertions |
| P5 | Recurring definitions (`_normalize_target`, `_workflow_bundle_for_target`, reconcile sweep in `api_service/services/recurring_workflows_service.py`) incl. legacy `kind: manifest_run` form | producer + control (scheduled/triggered/backfill/catch-up/resume) | `recurring_workflow_definitions.target` (`workflowType` or legacy `kind`) | `_normalize_target` rejects `ManifestIngest` and `manifest_run` actionably before dispatch; `_workflow_bundle_for_target` double-guards direct callers; reconcile pauses existing retired schedules, never (re)creates | Reject new/updated definitions; pause (never recreate/update) existing retired schedules; paused ≠ stopped for already-created executions (MR2 drain owns children). Do not convert to UserWorkflow. Child: MR1 (#4188) | `test_create_definition_rejects_retired_manifest_ingest` + legacy-`kind` rejection test (this change), reconcile-pause test incl. legacy kind |
| P6 | One-time migration `scripts/migrate_to_temporal_schedules.py` (`_workflow_type_for_target` `manifest_run` mapping) | producer (cutover migration) | Legacy definitions → Temporal Schedules | `_workflow_type_for_target` returns `None` for `manifest_run`/`ManifestIngest`; `migrate_definitions` skips with warning, leaves `temporal_schedule_id` unset | Skip retired targets (this change); never create a retired schedule, never convert to `MoonMind.Run`. Protected export owned by MR4/runbook-4191. Child: MR1 (#4188) | Migration-skip unit test (this change): `manifest_run` and `ManifestIngest` targets create no schedule and set no id |
| P7 | CLI (`moonmind/cli.py` manifest group, `moonmind/manifest/manifest_cli.py`) — REMOVED | producer (CLI authoring) | CLI-submitted executions | N/A — command group/imports absent; ordinary CLI (worker/container, runtime/profile, credentials, context) intact via shared P3 boundary | Remove group/imports; retired-product help text only. Coordinate #3939 (workflow CLI) / #4112 (vector CLI) without rebuilding. Child: MR1 (#4188) | `test_cli_help_advertises_no_manifest_command_group`; ordinary CLI flows green, no new mandatory setting |
| P8 | Frontend (`frontend/src/entrypoints/manifests.tsx` — REMOVED; `dashboard-app.tsx`, `dashboardRoutes.ts`, `workflow_console.py` incl. legacy `/manifests/new` redirect, boot/page unions, lazy imports, nav) | producer (UI authoring) + read (generic history) | Browser state / cached drafts (client only) | Routes/nav/imports/page types absent; stale client payloads re-enter P3 and fail visibly; existing drafts show actionable correction | Remove authoring surfaces; keep generic list/detail/artifact reads with historical `entry=manifest` evidence (P10). Child: MR1 (#4188) | `dashboard.test.tsx`, `dashboardRoutes.test.ts` (`/manifests` → null), `test_workflow_console.py` retired-route absence |
| P9 | Saved drafts, presets, copied/rerun inputs, `entry=manifest` list filter, explicit `MoonMind.ManifestIngest` payloads, legacy target/entry forms | producer (stale/copied authoring) | Draft blobs, preset definitions, copied parameters, schedule targets | Same P3/P4/P5 boundaries: explicit retired intent rejected actionably; never parsed away and run as `UserWorkflow` | Actionable correction for drafts; presets/schedules cannot reintroduce retired authority (cf. vector-field draft precedent `executions.py:11377`). Direct Temporal administration is an operator trust boundary owned by the cutover, not the HTTP validator. Child: MR1 (#4188) | Draft/preset/copied-input rejection tests surfacing the retired message (extends P3/P5 coverage) |
| P10 | Historical reads (`_resolve_execution_entry` → `manifest` for old rows, `ExecutionModel.manifestArtifactRef` lineage fallbacks, generic artifact APIs, `TemporalWorkflowType.MANIFEST_INGEST` enum retained for decoding) | read-only | Old execution rows, immutable inputs, artifact refs, lineage | No launch path: decoders never grant executability; creation boundaries (P3) reject before the type mapping | Preserve authorized generic list/detail/artifact reads; `entry=manifest` is evidence, not admission. Removal trigger for the enum: versioned DB migration with cutover plan (out of scope). Child: MR2 (#4189) | `test_describe_historical_manifest_execution_exposes_lineage_refs_only`, replay/drain rehearsal, degraded-status read-only regression |
| P11 | Schedules runtime (trigger/backfill/catch-up/resume of already-created executions) | control | Live Temporal Schedules + open/pending/retryable activities + children | Reconcile pause stops future fires; already-created executions owned by MR2 drain (finish on old release or deliberate cancel with child verification) | Pause ≠ proof stopped; MR2 inventory covers parents/children/pending controls per deployment before new-release deploy. Ordinary schedules unaffected. Child: MR2 (#4189) + MR4 | Reconcile + drain-gate tests (`manifest_ingest_drain`), ordinary-schedule regression |
| P12 | Arbitrary user content (`manifest.yaml` uploads, recovery/Skill/Vite manifests, user prose) | non-producer (must not match) | Generic artifacts / unrelated manifests | Generic artifact upload and normal Create/CLI/runtime/profile flows accept them as ordinary content | Target retired application contracts only. Child: none (negative guard) | Normal-flow regression: generic upload + UserWorkflow submit green; `verify_vite_manifest`-class tooling untouched |

Traceability: P1/P2/P7/P8 (removals) → "remove-authoring-surfaces" / "remove-router-cli" (met).
P3–P6/P9/P11 (rejection + schedule retirement + no-bypass) → "retire-producers-reject-before-effects" /
"retire-schedules-with-evidence" / "no-bypass-cached-copied-legacy" (partially-met; this
change closes the `manifest_run` migration/reactivation gap and records the ledger).
P10/P12 → "preserve-normal-flows" (met). No deployment mutation, new pipeline,
permanent compatibility alias, or #3948 feature work authorized.
