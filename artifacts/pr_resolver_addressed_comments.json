[
  {
    "id": 3810507762,
    "disposition": "addressed",
    "rationale": "tests/integration/workflows/temporal/test_omnigent_session_supervisor.py now imports the omnigent_session module once, via the module-alias `import from` form at the top of the file, and the function-local `import ... as session_module` was removed."
  },
  {
    "id": 3810507774,
    "disposition": "addressed",
    "rationale": "Same fix for agent_run: one top-level `from ... import agent_run as agent_run_module`, function-local duplicate import removed."
  },
  {
    "id": 3810507785,
    "disposition": "addressed",
    "rationale": "Added an explanatory comment on the `except (asyncio.TimeoutError, TimeoutError): pass` in _await_wake_or_snapshot_deadline: timing out is the normal no-wake path that falls through to the next scheduled snapshot."
  },
  {
    "id": 3810534147,
    "disposition": "addressed",
    "rationale": "P1. Provider-snapshot observation identity now includes the turn confirmation marker, so the quiet-period confirming read persists its own row (with providerTurn.turnComplete) instead of deduplicating against the earlier pending observation, while retries of the same read still dedupe. Covered by test_snapshot_requires_stable_marked_turn_before_idle_terminal, whose fake now enforces the real (session, dedup key) idempotency contract."
  },
  {
    "id": 3810534150,
    "disposition": "addressed",
    "rationale": "P2. Durable terminal mapping now derives from the reducer's own classify_provider_status via _durable_terminal_outcome, so timed_out/timeout classify as FAILURE and CANCELLED is reserved for explicit cancel states. Covered by test_durable_terminal_outcome_matches_reducer_classification."
  },
  {
    "id": 3810534154,
    "disposition": "addressed",
    "rationale": "P1. Added the omnigent.heartbeat_host_lease activity (registered in the catalog, runtime map, and runtime wrapper) and the supervisor now renews the host lease every poll cycle before observing, keeping renewal inside the janitor's 90s heartbeat timeout. Non-renewable, cleanup-claimed, and missing leases are reported for reconciliation rather than failing the session. Covered by test_poll_cycle_renews_host_lease_before_observing and test_heartbeat_activity_renews_only_a_renewable_lease."
  },
  {
    "id": 3810534159,
    "disposition": "addressed",
    "rationale": "P1. omnigent_stop_host_activity now claims cleanup authority via _claim_host_cleanup_authority (claim_host_lease_cleanup compare-and-swap on the observed status and heartbeat) and skips stop/delete entirely when another owner won the fence. Covered by test_stop_host_does_not_run_cleanup_it_did_not_win."
  },
  {
    "id": 3810534164,
    "disposition": "addressed",
    "rationale": "P1. The profile lease request now sends workflowId (the supervisor's own durable workflow ID), stepExecutionId, idempotencyKey, and ownerIsWorkflow=False, all of which survive the manager's metadata allowlist, so acquire_slot_v2 can verify the owning workflow instead of raising ProviderProfileLeaseOwnerMissing. Covered by test_profile_lease_request_carries_owning_workflow_authority."
  },
  {
    "id": 3810534166,
    "disposition": "addressed",
    "rationale": "P2. _queue_signal_intent no longer raises on a full backlog: it deduplicates by (kind, requestId), admits one cancellation and one cleanup intent past the bound so recovery intents are never lost, and otherwise counts a bounded droppedIntentCount exposed on the state query. Covered by test_full_signal_backlog_never_throws_from_a_signal_handler and test_repeated_signal_request_id_is_deduplicated_not_requeued."
  },
  {
    "id": 4969010602,
    "disposition": "not-applicable",
    "rationale": "Codex review summary body with no specific requested change; its individual findings are tracked as the review comments above."
  },
  {
    "id": 3810002054,
    "disposition": "addressed",
    "rationale": "Removed the unnecessary lambda wrapper in tools/omnigent_exact_artifact_probe.py; _probe_import already returns the callable."
  },
  {
    "id": 3810002061,
    "disposition": "addressed",
    "rationale": "Dropped the unused Iterable import from moonmind/omnigent/exact_artifact_conformance.py."
  },
  {
    "id": 3810002064,
    "disposition": "addressed",
    "rationale": "Dropped the unused Iterable import from moonmind/omnigent/live_verification_health.py."
  },
  {
    "id": 3810006712,
    "disposition": "addressed",
    "rationale": "Probe assets now reach the deployable image through an explicit read-only /probe mount with PYTHONPATH=/app, so no test-only asset is baked into the production image and imports still resolve from the artifact under test. The browser controller's Playwright runtime is installed on the CI host and drives the container's hosted origin. Docker runs now use the locally resolvable sha256:<image id> content id, which fixes the 'pull access denied' failure the unpullable repo digest caused."
  },
  {
    "id": 3810006715,
    "disposition": "addressed",
    "rationale": "The exact-artifact job now starts temporalio/auto-setup (the same image and configuration canonical Compose uses) on the job's PostgreSQL and waits for 127.0.0.1:7233; the worker probe requires an explicit --temporal-address and passes TEMPORAL_ADDRESS plus the POSTGRES_* settings the image actually reads."
  },
  {
    "id": 3810006717,
    "disposition": "addressed",
    "rationale": "Each migration scenario now runs against its own freshly created database (reset_database drops and recreates moonmind_exact_artifact_clean / _prior), and the prior-schema case materializes the real revision preceding head via 'alembic show head' and then upgrades, instead of stamping base over an already-migrated schema."
  },
  {
    "id": 3810006720,
    "disposition": "addressed",
    "rationale": "The fabricated booleans are gone: the fake-provider probe and the fakeProviderExecution gate section were removed rather than kept on an unrelated exit status. In their place the server probe performs a real bounded execution boundary it can observe -- docker restart of the deployable container against the schema it just migrated, requiring liveness again -- surfaced as the new required capability api_restart_against_existing_schema. Restart/terminal replay after fake-host removal is now explicitly documented as owned by the reliability-journey and embedded-recovery gates that actually exercise it, which the Omnigent contract gate selects on the same commit."
  },
  {
    "id": 3810006726,
    "disposition": "addressed",
    "rationale": "assemble_omnigent_live_status now retrieves the acceptance manifest published by the newest passing live-conformance run (select_manifest_artifact + gh run download) before evaluation, with an explicit --manifest override; a genuine absence still yields None so the projection stays fail-closed."
  },
  {
    "id": 3810006731,
    "disposition": "addressed",
    "rationale": "Added the canonical redact_secrets in moonmind/omnigent/conformance.py whose patterns span the complete credential -- the whole session-bearing header value to end of line, JWTs, cookies, bare Bearer/Basic credentials, and token bodies after known prefixes -- so redaction is no longer a header-name-only strip. Redaction runs before line truncation, assert_secret_free now also rejects the wider set of credential-bearing keys, and tests assert the published logTail passes an independent secret scan."
  },
  {
    "id": 3810006732,
    "disposition": "addressed",
    "rationale": "Added the real production API command api_service/entrypoint.sh to EXACT_ARTIFACT_EXACT, replaced the nonexistent api_service/docker/entrypoint.sh in the selector test with real repository paths, and added a guard test asserting every exact-path inventory entry exists so a typo cannot mask a coverage gap again. Also removed the nonexistent uv.lock entry and the compose entries the globs already cover."
  },
  {
    "id": 3810006739,
    "disposition": "addressed",
    "rationale": "Compose now bind-mounts ${MOONMIND_OMNIGENT_EVIDENCE_DIR:-./var/omnigent-evidence} read-only at /workspace/omnigent-evidence and defaults all three evidence refs to files inside it, so the default deployment consumes real evidence with no per-file configuration. tools/materialize_omnigent_evidence.py is the automatic materialization path: it copies the newest unexpired document published for the deployed commit into that directory, refusing evidence for another commit. Compose defaults and the mount are pinned by test_compose_foundation."
  },
  {
    "id": 3810006744,
    "disposition": "addressed",
    "rationale": "Added assert_live_health_projection and used it at the catalog consumption boundary: it validates the versioned schema, the ready verdict, the deployed commit, the projection's own age against the hourly publication cadence, and the acceptance expiry it inherits. Tests cover expired acceptance windows, a stalled publisher, and a foreign schema."
  },
  {
    "id": 3810006749,
    "disposition": "addressed",
    "rationale": "Both migration commands now pass the repository's explicit configuration (-c api_service/migrations/alembic.ini), matching the alembic-migration-gate invocation, and run with -w /app so script_location resolves."
  },
  {
    "id": 3810006755,
    "disposition": "addressed",
    "rationale": "The server and UI probes share one container lifetime: _probe_server_and_ui starts the API container, probes routes, runs the browser capture against that live hosted origin, then performs the restart probe before teardown. A test asserts the capture happens between container start and removal, and that the UI signals fail closed when the API never starts."
  },
  {
    "id": 3810006759,
    "disposition": "addressed",
    "rationale": "The hosted capture no longer counts any same-origin request. It now requires three concrete facts: a request under the image's compiled-bundle mount /static/workflow_console/dist/, a parseable #moonmind-ui-boot payload, and rendered content in #dashboard-app-root. A hermetic contract test pins each selector and the bundle prefix to the real template and static mount."
  },
  {
    "id": 3810006765,
    "disposition": "addressed",
    "rationale": "aggregate_provider_runner_fleet replaces first-match selection: the tier is online when at least one labeled runner is online, and busy only when no online runner is idle, with matching/online/idle counts reported. Tests cover an offline runner ordered ahead of an online one and mixed busy/idle fleets."
  },
  {
    "id": 3823708555,
    "disposition": "addressed",
    "rationale": "Removed unused imports in agent_profile.py; schemaVersion now constrained to v2 literal."
  },
  {
    "id": 3823732803,
    "disposition": "addressed",
    "rationale": "Generic realizer wiring is via planner trusted selection and support keys; added runtime vendor refs and full agent source digest to support payload, and planner now enforces via validate_realizer."
  },
  {
    "id": 3823732811,
    "disposition": "addressed",
    "rationale": "Planner now binds trust to exact implementation: checks trust.implementationRef == profile.harness.implementationRef and catalog implementation."
  },
  {
    "id": 3823732840,
    "disposition": "addressed",
    "rationale": "Planner now rejects launch policies outside profile allowlist via allowedLaunchPolicyRefs check."
  },
  {
    "id": 3823732862,
    "disposition": "addressed",
    "rationale": "Runtime binding now verifies digest via model_validator recomputing expected ref."
  },
  {
    "id": 3823732869,
    "disposition": "addressed",
    "rationale": "Credential binding validation now rejects extra slots via declared_slots check."
  },
  {
    "id": 3823732891,
    "disposition": "addressed",
    "rationale": "Realizer selection now trusted: ignores caller override except for explicit codex compatibility, enforced via planner."
  },
  {
    "id": 3823732899,
    "disposition": "addressed",
    "rationale": "Attestation now includes architecture field and validation checks expectedArchitecture."
  },
  {
    "id": 3823732927,
    "disposition": "addressed",
    "rationale": "Host Class registration now rejects duplicate ref with different definition, requiring new version."
  },
  {
    "id": 3823732934,
    "disposition": "addressed",
    "rationale": "Skill artifact ref now uses full digest (artifact:sha256:...) for collision resistance."
  },
  {
    "id": 3823732945,
    "disposition": "addressed",
    "rationale": "Planner now binds catalog to endpoint: checks profile.endpointRef == catalog.endpointRef and catalogRef equality."
  },
  {
    "id": 3823732957,
    "disposition": "addressed",
    "rationale": "Planner now validates bindings against slot acceptedAuthModels and provider constraints."
  },
  {
    "id": 3823732977,
    "disposition": "addressed",
    "rationale": "Attestation validator now requires expectedHostId and binds to leased host identity."
  },
  {
    "id": 3823732999,
    "disposition": "addressed",
    "rationale": "Materializer now derives accessMode from state.mutable: writable for OAuth homes."
  },
  {
    "id": 3823733007,
    "disposition": "addressed",
    "rationale": "Support classification now requires has_experimental_evidence for experimental, otherwise discovered_only."
  },
  {
    "id": 3824599640,
    "disposition": "addressed",
    "rationale": "Removed unused top-level hashlib import; now imported locally where used for support key generation."
  },
  {
    "id": 4968365371,
    "disposition": "addressed",
    "rationale": "Fixed conflict: merged main ledger entries with harness platform entries."
  },
  {
    "id": 3810488308,
    "disposition": "addressed",
    "rationale": "Unused local `resumed` in test_partial_failure_then_resume_without_duplication now carries a meaningful assertion (resumed.applied >= 1)."
  },
  {
    "id": 3810488322,
    "disposition": "addressed",
    "rationale": "Replaced the bare `...` body of CanonicalMigrationRepository.is_applied with a docstring (no ineffectual statement), matching the sibling protocol methods."
  },
  {
    "id": 3810488336,
    "disposition": "addressed",
    "rationale": "Replaced the bare `...` body of CanonicalMigrationRepository.record_applied with a docstring."
  },
  {
    "id": 3810488345,
    "disposition": "addressed",
    "rationale": "Replaced the bare `...` body of CanonicalMigrationRepository.has_canonical_session with a docstring."
  },
  {
    "id": 3810488353,
    "disposition": "addressed",
    "rationale": "Replaced the bare `...` body of CanonicalMigrationRepository.has_chat_alias with a docstring."
  },
  {
    "id": 3810488361,
    "disposition": "addressed",
    "rationale": "Replaced the bare `...` body of CanonicalMigrationRepository.is_quarantined with a docstring."
  },
  {
    "id": 3810511293,
    "disposition": "addressed",
    "rationale": "Added a fail-closed `supervisor_workflow_registered` readiness gate so enabling the supervisor flags fails admission closed (reason supervisor_workflow_not_registered) until the production MoonMind.OmnigentSession workflow is actually registered/wired, rather than silently admitting nothing. Full production Temporal workflow registration + launch-boundary invocation is tracked as the next #3712 integration step; this change makes the interim exposed settings safe-by-construction."
  },
  {
    "id": 3810511299,
    "disposition": "addressed",
    "rationale": "verify mode now checks a persisted cleanup marker (new has_cleanup_marker repository predicate) for mark_cleanup_required actions and reports missing_cleanup_marker discrepancies."
  },
  {
    "id": 3810511306,
    "disposition": "addressed",
    "rationale": "providerCapacityReleaseAllowed now requires both credential_consumers_stopped AND a new cleanup_completed terminal-evidence field, matching the reconciler release policy; a stopped consumer alone no longer authorizes release."
  },
  {
    "id": 3810511309,
    "disposition": "addressed",
    "rationale": "diagnostic_ref now has a field validator requiring a bounded, validated canonical `artifact://` reference (safe charset + length cap); raw provider-session/credential-looking values are rejected before they can be emitted into reports."
  },
  {
    "id": 3810511312,
    "disposition": "addressed",
    "rationale": "Machine-checkable refs now name concrete `module:symbol` targets and _ref_resolves resolves the symbol via getattr, so deleting a still-required class/function fails the guard even if its module remains; bare module refs are rejected."
  },
  {
    "id": 3810511314,
    "disposition": "addressed",
    "rationale": "rollback_metadata now emits reversal entries only for actions whose idempotency marker is present in the repository (await repo.is_applied), so a partial-failure run no longer lists unapplied side effects."
  },
  {
    "id": 3810511320,
    "disposition": "addressed",
    "rationale": "classify_record now routes a record that has a canonical session but still has an outstanding chat alias to ALIAS_REQUIRED (alias-producing) instead of new_model_ready/noop."
  },
  {
    "id": 4968981568,
    "disposition": "not-applicable",
    "rationale": "Informational Codex review summary body; individual actionable suggestions are tracked as their own review-comment threads above."
  },
  {
    "id": 3819559431,
    "disposition": "addressed",
    "rationale": "The activity now accepts only objective cleanupResult values before marking the host lease stopped. OmnigentOAuthHostRuntime.stop_host already raises OMNIGENT_HOST_CLEANUP_INCOMPLETE when the container, owned volumes, or runtime files remain, and the new activity regression proves a failed cleanup result leaves the validation lease nonterminal."
  },
  {
    "id": 3819559440,
    "disposition": "addressed",
    "rationale": "Bound-host revalidation now captures provider-login failures, always invokes lease-owned host cleanup, marks the lease stopped only after cleanup succeeds, and then propagates the original preflight failure. The activity regression exercises this failure ordering."
  },
  {
    "id": 3819559447,
    "disposition": "addressed",
    "rationale": "OAuth revalidation no longer creates or passes an empty execution workspace. The credential-only preflight mounts only the approved OAuth volume and runs the provider login-status command in the selected host image; its runtime test asserts that /workspaces/run is absent."
  },
  {
    "id": 3819559454,
    "disposition": "addressed",
    "rationale": "OAuth revalidation no longer waits for Omnigent host registration. The credential-only provider login check has a 60-second bound, leaving the existing 180-second activity budget for deterministic cleanup."
  },
  {
    "id": 4980223622,
    "disposition": "not-applicable",
    "rationale": "Automated review summary only; the five concrete inline findings from this review are classified separately."
  },
  {
    "id": 3819559461,
    "disposition": "addressed",
    "rationale": "The activity no longer calls the full prepare_host execution contract. The new credential-only boundary validates the durable effective-launch image and binding identity but does not require an execution workspace, resolved Skill snapshot, artifact gateway, or fabricated execution authority."
  }
]
