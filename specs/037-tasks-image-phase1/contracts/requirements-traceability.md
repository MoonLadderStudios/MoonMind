# Requirements Traceability – Tasks Image Attachments Phase 1

| Source Requirement | Functional Requirement | Planned Implementation Surface | Validation Strategy |
|--------------------|------------------------|--------------------------------|---------------------|
| DOC-REQ-001 – Create job with attachments | FR-001 | `api_service/api/routers/agent_queue.py::create_job_with_attachments`, `moonmind/workflows/agent_queue/service.py::create_job_with_attachments`, queue events | Unit tests in `tests/unit/api/routers/test_agent_queue.py::test_create_job_with_attachments*`; service async tests in `tests/unit/workflows/agent_queue/test_service_attachments.py` plus new integration scenario exercising job visibility after uploads |
| DOC-REQ-002 – Validate + store `inputs/` artifacts | FR-003, FR-005 | Attachment sanitization + digesting in AgentQueueService, `.moonmind/inputs` downloads in worker prepare | Unit + worker tests verifying type sniffing, size limits, `.moonmind/inputs` contents, manifest digest matching |
| DOC-REQ-003 – User + worker list/download endpoints | FR-002 | New `GET /api/queue/jobs/{id}/attachments` and `/attachments/worker` routers, repository filters | API router tests covering authz; service tests for `_list_input_artifacts` + `_assert_job_worker_ownership` |
| DOC-REQ-004 – Reserve namespace / auth | FR-004 | Artifact upload guard rejecting `inputs/`, ACL in list/download endpoints | Regression tests ensuring worker uploads fail + unauthorized user/worker list/download attempts raise errors |
| DOC-REQ-005 – Worker downloads + events | FR-005, FR-006 | Codex worker prepare stage extension (manifest, `.moonmind/inputs`, task_context updates, queue events) | Worker unit tests verifying manifest schema, `.git/info/exclude`, emitted events; integration logs checking manifest + context artifacts |
| DOC-REQ-006 – Prompt injection | FR-008 | `_compose_step_instruction_for_runtime` + runtime wrappers insert `INPUT ATTACHMENTS` block referencing manifest/context | Unit tests for prompt builder (Codex/Gemini/Claude) verifying block order and disabled-message fallback |
| DOC-REQ-007 – Vision module | FR-007 | New `moonmind/vision` settings + service wrappers for caption/OCR; `.moonmind/vision/image_context.md` writer | Unit tests for vision module (provider flag, disabled mode) + worker tests for file contents |
| DOC-REQ-008 – Dashboard UI | FR-009 | Dashboard form updates (drag/drop, validation), job-detail attachments panel consuming new endpoints | Frontend tests (React/Vite) + manual QA steps; optional Playwright test hitting attachments preview/download |
| DOC-REQ-009 – Storage layout | FR-010 | `AgentQueueArtifactStorage` writes under `var/artifacts/agent_jobs/<jobId>/inputs/...`; worker downloads `.moonmind/inputs` | Service tests assert storage paths; worker tests ensure `.moonmind` paths excluded from git |
| DOC-REQ-010 – Security logging + limits | FR-003, FR-004, FR-011 | Limits + sanitized filenames; queue events for upload/download/context | Unit tests for limit enforcement and event payloads |
| DOC-REQ-011 – Testing scope | FR-012 | Expand `./tools/test_unit.sh` coverage, add worker/integration tests | CI gating ensures new tests run in unit + orchestrator suites |
