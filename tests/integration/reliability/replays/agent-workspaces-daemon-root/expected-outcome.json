{
  "invariant": "the worker resolves the remote-daemon root by inspecting the Compose named volume in the selected Docker daemon",
  "volumeNameTemplate": "${MOONMIND_AGENT_WORKSPACES_VOLUME_NAME:-agent_workspaces}",
  "daemonRootSource": "docker volume inspect --format {{.Mountpoint}}"
}
