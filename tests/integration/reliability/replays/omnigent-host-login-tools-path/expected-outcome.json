{
  "invariant": "the login-shell PATH snippet is image-owned at /etc/profile.d/moonmind-tools.sh, so no bind mount overlays it; the launch still leads PATH with the image-owned tools directory for ordinary processes",
  "sourceName": "moonmind-tools.sh",
  "containerPath": "/etc/profile.d/moonmind-tools.sh"
}
