{
  "_comment": "Duration hints for duration-balanced reliability sharding (MoonLadderStudios/MoonMind#4367). Advisory optimization input only: files missing here (including newly added tests) still run via DEFAULT_WEIGHT_SECONDS. Never gate on exact counts, file names, or freshness of this file. Estimates below seed from CI run #14404 (616 tests in 1219.12s; ~14m23s across eight slow cases) and are refined from the per-shard durations snapshots uploaded by tools/ci/write_backend_matrix_summary.py.",
  "DEFAULT_WEIGHT_SECONDS": 20,
  "weights": {
    "test_automatic_release_availability.py": 280,
    "test_release_routing_journey.py": 260,
    "test_cancel_unserved_worker_version.py": 110,
    "test_interrupted_host_admission_journey.py": 110,
    "test_checkpoint_cold_resume.py": 60,
    "test_omnigent_fault_corpus.py": 60,
    "test_worker_replacement_journey.py": 60,
    "test_escaped_failure_journeys.py": 60
  }
}
